using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class door1check : MonoBehaviour
{
    // GameObject scene;

    // testroom1 level;

    // Start is called before the first frame update
    void Start()
    {
        // scene = GameObject.Find("test_level");
        // level = scene.GetComponentInParent<testroom1>();
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    // public void OnTriggerEnter2D(Collider2D col)
    // {
    //     // level.move_to_scene = 1;
    //     // Debug.Log($"Hit door: {level.move_to_scene}");
    // }

}
